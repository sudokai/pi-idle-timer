{
  "name": "pi-idle-timer",
  "version": "2.0.0",
  "type": "module",
  "description": "Status-bar idle timer for pi and prime-agent: time since the agent finished its last response",
  "keywords": [
    "pi-package",
    "prime-agent",
    "statusbar",
    "timer"
  ],
  "pi": {
    "extensions": [
      "./extensions/idle-timer.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/format-idle-seconds.test.ts tests/runtime-compat.test.ts"
  }
}
